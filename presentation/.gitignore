# The rendered film and the voice track are build outputs, not sources. Both are
# reproducible: capture/record.* renders the picture, narrate.py renders the voice.
capture/*.mp4
capture/*.mkv
capture/picture.*
film/vo/
__pycache__/
capture/work/
capture/*.log
capture/picture.mp4
